let arr=[1,1,2,2,3,3]
let count=0;
let dupli1;
let dupli2;
let dupli3;
{
    for (i=0;i<arr.length;i++)
    {
    for (j=0;i<arr.length;j++)
    {
       if (arr[i]==arr[j])
       {
           count += i;
           dupli1=arr[0];
       }
       else
    }
    }
}